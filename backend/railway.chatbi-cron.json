{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m app.jobs.chatbi_rollup",
    "cronSchedule": "30 16 * * *",
    "restartPolicyType": "NEVER"
  }
}
